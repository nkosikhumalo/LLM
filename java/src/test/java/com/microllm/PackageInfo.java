package com.microllm;

/**
 * Belongs here: unit tests for tensor ops, softmax, gradient checks, layer shapes, export round-trips.
 *
 * Add concrete *Test.java classes alongside implementation work.
 */
class PackageInfo {
}
