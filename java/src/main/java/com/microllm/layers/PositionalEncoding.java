package com.microllm.layers;

/**
 * Belongs here: positional encoding — adds order awareness to token vectors.
 *
 * Intended contents:
 * - Sinusoidal encodings, OR learned absolute position embeddings
 * - Forward: x + position vectors (up to max sequence length)
 *
 * Used by: model.Transformer (right after Embedding)
 */
public class PositionalEncoding {
}
