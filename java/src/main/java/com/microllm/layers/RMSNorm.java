package com.microllm.layers;

/**
 * Belongs here: RMSNorm — root-mean-square normalization (no mean centering / often no bias).
 *
 * Intended contents:
 * - Learnable scale
 * - Forward + backward
 *
 * Optional alternative to LayerNorm for the micro Transformer.
 */
public class RMSNorm {
}
